{
  "programId": {
    "description": "Internally generated unique identifier of an academic program definition.",
    "type": "object",
    "pattern": "^[0-9]{5}$",
    "required": [
      "value"
    ],
    "properties": {
      "value": {
        "type": "string",
        "maxLength": 5
      },
      "api_type": {
        "type": "string",
        "enum": [
          "system"
        ],
        "default": "system"
      },
      "key": {
        "type": "boolean"
      },
      "display_label": {
        "type": "string",
        "default": "Academic Program ID"
      }
    },
    "example": {
      "value": "12345",
      "api_type": "system",
      "display_label": "Program Id"
    }
  }
}